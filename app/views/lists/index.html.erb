<div class="navmams">
<h1>mamoun au ciné <strong>TRANQUILLE</strong></h1>
</div>

<div class="d-flex justify-content-center align-items-center">
<%= link_to 'add list', new_list_path, class: "btn btn-primary ahoui" %>
</div>

<div class="container">
  <div class="row">
   <% @lists.each do |list| %>
    <div class="col-md-6 col-sm col-lg-4 ">
      <div class="card-category">
        <h2><%= link_to list.name, list_path(list) %></h2>
      </div>
    </div>
    <% end %>
  </div>
</div>
